describe("The UI of the chat", () => {
  it.todo("displays a list of messages");

  it.todo("shows a list of reactions for each message");

  it.todo("can differentiate a user reaction");

  it.todo("has a profile pic next to the message");
});

describe("the interactions with the chat", () => {
  it.todo("can add a user reaction");

  it.todo("can remove a user reaction");
});
